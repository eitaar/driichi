# Use broadcast-noir UI with an accessibility baseline

The Frontend uses a dark broadcast-noir visual direction so table information remains legible at broadcast scale. v1 requires semantic DOM controls, usable contrast, visible keyboard focus, and reduced-motion fallbacks for the shell and controls. Mobile gameplay layout and full Pixi keyboard/screen-reader narration remain deferred; Pixi continues to render the visual table while React owns accessible controls and status text.
