# Separate Participant identity, presence, and control

A Participant's immutable Human, MJAI, MCP, or Built-in kind is independent of connection presence, Lobby selection, Match role, Seat, and Controller. Temporary Auto permits reconnect, Permanent Auto retains original Match identity while forbidding return, one active connection owns each interactive Participant, and global irreversible Bot Token revocation removes non-Players and permanently transfers every active authenticated Seat.
